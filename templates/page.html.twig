{% extends "index.html.twig" %}

{% block body %}
    <section id="text-1col">
        <div class="container">
            <div class="row">
                <h2 class="title text-center">{{ page.title }}</h2>
                {{ page.content|raw }}
            </div>
        </div>
    </section>
{% endblock %}