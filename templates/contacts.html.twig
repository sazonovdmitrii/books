{% extends "index.html.twig" %}

{% block body %}
    <section id="text-1col">
        <div class="container">
            <div class="row">
                <h2 class="title text-center">{{ contacts_block.title}}</h2>
                {{ contacts_block.content|raw }}
            </div>
        </div>
    </section>


{% endblock %}